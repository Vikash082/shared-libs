@Library('mylibrary') _

pipeline {
    agent any
    stages {
        stage('Experiment') {
            steps {
                echo "Hi there!"
                hello "Vikash"
            }
        }
    }
}
